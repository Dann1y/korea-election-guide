# 기여 가이드

이 프로젝트는 시민이 6·3 지방선거 정보를 더 쉽게 접하도록 돕는 비영리 도구입니다.
어떤 기여도 환영합니다.

## 시작 전 점검

1. 이슈를 먼저 검색해 중복인지 확인해주세요.
2. 큰 변경(새 페이지·새 의존성 등)은 이슈로 먼저 논의해주세요.
3. **시크릿/PII를 절대 커밋하지 마세요.** `.env`, API 키, 이메일, 전화번호 등.

## 개발 환경

```bash
pnpm install
cp .env.example .env  # NEC API 키 입력
pnpm dev
```

빌드·타입 체크:

```bash
pnpm build
```

## 코드 스타일

- **TypeScript strict** — `any` 지양, 도메인 타입은 `src/types/`에 통일
- **Tailwind utility-first** — 인라인 스타일 지양 (정당 색상 등 동적 값 제외)
- **모바일 우선** — `375px` viewport에서 가로 스크롤 0 유지
- **한국어 UI 우선** — 모든 사용자 노출 문자열은 한국어. 기술 식별자만 영어

## 데이터 출처 원칙

이 프로젝트는 **공식 1차 자료**만 사용합니다.

- ✅ 중앙선거관리위원회 공공데이터 OpenAPI
- ✅ 매니페스토실천본부 등 검증된 평가 기관(외부 링크로 안내)
- ❌ 검증되지 않은 LLM 생성 텍스트
- ❌ 출처 불명의 논란·평가 점수
- ❌ 특정 정당·후보 편향 콘텐츠

후보자 논란·이행률 점수처럼 표준화된 1차 자료가 없는 정보는
**의도적으로 표시하지 않습니다.** 시민이 1차 자료를 직접 확인하도록
외부 링크를 제공합니다.

## PR 가이드

1. fork → 기능 브랜치 생성 (`feat/...`, `fix/...`, `docs/...`)
2. 변경사항은 작게 자주 — 한 PR에 한 주제
3. PR 설명에 **무엇을·왜·검증 방법** 포함
4. 스크린샷(UI 변경) 또는 dev 콘솔 로그(API 변경) 첨부 권장
5. `pnpm build` 통과 확인

## 정치적 중립

본 프로젝트는 특정 정당·후보를 지지하거나 반대하지 않습니다.
다음과 같은 기여는 거부됩니다.

- 특정 후보·정당을 우대하거나 폄하하는 UI 변경
- 검증되지 않은 텍스트로 후보를 평가하는 콘텐츠
- 출처 없이 "현역", "공약 이행률" 같은 라벨을 임의로 부여

## 행동 강령

서로 존중하며 토론합니다. 인신공격·차별·정치적 모욕은 금지됩니다.

## 라이선스

기여한 코드는 [MIT 라이선스](./LICENSE) 하에 공개됩니다.
